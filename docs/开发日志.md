# 实验助手项目开发日志

> 用途：论文写作与答辩材料沉淀。  
> 维护方式：单一主日志长期维护，按“开发主题迭代”顺序追加。  
> 维护要求：每次功能开发完成后，提交代码前必须同步更新本日志。

## 目录
- [迭代 01 - 部署链路重构与容器化收敛](#迭代-01---部署链路重构与容器化收敛)
- [迭代 02 - Java17 升级与后端启动兼容修复](#迭代-02---java17-升级与后端启动兼容修复)
- [迭代 03 - 登录鉴权链路修复与验证码下线](#迭代-03---登录鉴权链路修复与验证码下线)
- [迭代 04 - 执行计划链路修复与真实案例联调](#迭代-04---执行计划链路修复与真实案例联调)
- [迭代 05 - 个人信息与平台管理页面改造](#迭代-05---个人信息与平台管理页面改造)
- [下一阶段优化任务清单（不含 Python 端）](#下一阶段优化任务清单不含-python-端)
- [日志更新规范（强制）](#日志更新规范强制)
- [迭代记录模板](#迭代记录模板)

---

## 迭代 01 - 部署链路重构与容器化收敛
### 目标与验收
- 目标：形成可稳定复现的本地部署路径，解决“镜像来源不一致、启动失败、前端构建受网络代理影响”问题。
- 验收：`webapp` 与 `exphlp_front` 能正常启动，`/api/auth/healthz` 与 `/api/auth/captcha` 可访问。

### 关键改动
- 将部署相关文件统一收敛到 `docker/`，文档统一收敛到 `docs/`。
- 重写 `docker/docker-compose.yml` 与 `docker/docker-compose.local-front.yml`，支持“全容器构建”和“本地dist兜底”。
- 新增 `docker/scripts/preflight.ps1` 进行镜像、端口、jar产物检查。
- 新增 `docs/新版部署方法.md`，固化启动步骤与故障速查。

### 重难点与报错闭环
- 报错：`base name (${FRONT_NGINX_IMAGE}) should not be blank`
  - 根因：环境变量缺失。
  - 修复：补齐 `.env` 必填键，并在预检脚本强校验。
- 报错：`npm ci ECONNREFUSED 127.0.0.1:7897`
  - 根因：容器内继承了不可用代理。
  - 修复：提供本地构建前端+挂载dist的兜底方案。
- 结果：前端与后端容器可稳定启动，部署流程可复现。

---

## 迭代 02 - Java17 升级与后端启动兼容修复
### 目标与验收
- 目标：后端统一升级到 Java 17 并消除类版本不兼容。
- 验收：`c_exphlp_webapp` 启动成功，Nginx 不再 502。

### 关键改动
- Maven 构建与运行时统一 Java 17。
- Spring 体系升级到兼容 Java17 的版本组合（Boot 2.7.x 等）。
- 增加 `spring.mvc.pathmatch.matching-strategy=ant_path_matcher` 兼容旧 swagger 行为。

### 重难点与报错闭环
- 报错：`Unsupported class file major version 61`
  - 根因：JDK17 编译产物被旧 Spring 版本运行。
  - 修复：升级 Spring Boot/Framework 依赖并重建镜像。
- 结果：`webapp` 容器启动恢复，健康检查通过。

---

## 迭代 03 - 登录鉴权链路修复与验证码下线
### 目标与验收
- 目标：解决“登录后立刻401”“登录状态已过期循环弹窗”“undefined.data”报错；按需求下线验证码。
- 验收：`/api/auth/login -> /api/auth/me` 连续返回 200，登录页无验证码。

### 关键改动
- 前端 `request.js` 在 401 分支显式 `reject`，避免连锁JS异常。
- 登录流程移除验证码字段与接口依赖（`code/uuid/getCodeImg`）。
- 后端 `currentUser` 增加兼容兜底：按 `userId` 查不到时按 `userName` 查。
- 用户主键注解统一为 `@Id`，降低 Mongo 映射差异风险。

### 重难点与报错闭环
- 报错：`TypeError: Cannot read properties of undefined (reading 'data')`
  - 根因：401 分支只弹窗不 reject。
  - 修复：401 分支返回 `Promise.reject` 并跳转 `/login`。
- 报错：`/api/auth/me -> 401 未登录（login已200）`
  - 根因：用户ID查询与历史数据 `_id` 类型不一致。
  - 修复：DAO 双ID兼容 + `currentUser` 按用户名兜底。

---

## 迭代 04 - 执行计划链路修复与真实案例联调
### 目标与验收
- 目标：修复“计划执行失败、planId空、结果查询500”问题，跑通真实案例脚本。
- 验收：`execute` 受理成功，状态可收敛，结果查询接口不再NPE。

### 关键改动
- 修复执行计划实体ID映射与DAO查询逻辑（字符串/ObjectId兼容）。
- 修复 `AlgRltSaveController` 空结果空指针。
- 补充真实案例文档脚本：时间戳命名、planId兜底、失败即停。

### 重难点与报错闭环
- 报错：`execute planId不能为空`
  - 根因：`addExePlan`失败后仍继续执行后续步骤。
  - 修复：脚本增加显式校验与兜底查询。
- 报错：`问题实例不存在: <id>`
  - 根因：问题实例ID查询仍按单一类型匹配。
  - 修复：`ProbInstDao/AlgLibMgrDao` 增加双ID兼容。

---

## 迭代 05 - 个人信息与平台管理页面改造
### 目标与验收
- 目标：修复头像上传401，去掉不合理字段与敏感信息展示，新增手机号/QQ字段。
- 验收：头像上传成功；个人信息与平台管理可读写手机号/QQ；平台管理不再显示密码密文与角色列。

### 关键改动
- 鉴权策略修复：`POST /api/auth/avatar` 需登录；`GET /api/auth/avatar/*` 允许公开读取。
- 个人中心字段整理：移除部门/角色/性别；新增手机号、QQ。
- 平台管理：移除角色列，新增手机号/QQ列；保留重置密码独立操作。
- 后端 `UserInfo` 新增字段：`mobile`、`qq`，并同步 profile/plat 接口返回与更新。

### 重难点与报错闭环
- 报错：提交头像后弹“登录状态已过期”
  - 根因：错误放行 `/api/auth/avatar/**` 导致上传请求无认证上下文。
  - 修复：调整拦截器，仅放行 GET 头像读取。
- 风险：删除角色列后新增/编辑用户可能丢失角色。
  - 处理：后端新增默认角色与更新时角色兜底（沿用原值）。

---

## 下一阶段优化任务清单（不含 Python 端）
### P0
- 统一错误码与前端错误提示映射，减少“提示文案不一致”。
- 为登录、头像上传、计划执行加入结构化日志字段（traceId/userId/path/errorCode）。
- 补认证链路与用户管理链路回归测试（最小自动化）。

### P1
- 清理模板残留接口引用（`/admin/*`）并输出接口总览文档。
- 完善平台管理表单校验与字段约束（邮箱/手机号/QQ）。
- 增加运维脚本：一键 smoke（登录、me、头像上传、计划执行最小链路）。

---

## 日志更新规范（强制）
1. 每次开发完成后，提交代码前必须追加一条迭代记录。  
2. 若改动涉及接口或数据结构，必须更新“接口变化”描述。  
3. 若出现新报错，必须写清“现象-根因-修复-验证”。  
4. 所有命令与路径使用可复现形式，避免仅截图不留文本。  

---

## 迭代记录模板
```markdown
## 迭代 XX - <主题名称>
### 目标与验收
- 目标：
- 验收：

### 关键改动
- 前端：
- 后端：
- 部署/文档：

### 重难点与报错闭环
- 报错：
  - 根因：
  - 修复：
  - 验证：

### 接口/数据结构变化
- 

### 风险与后续
- 
```

