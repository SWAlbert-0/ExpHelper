# 实验助手项目开发日志

> 用途：论文写作与答辩材料沉淀。  
> 维护方式：单一主日志长期维护，按“开发主题迭代”顺序追加。  
> 维护要求：每次功能开发完成后，提交代码前必须同步更新本日志。

## 目录
- [迭代 01 - 部署链路重构与容器化收敛](#迭代-01---部署链路重构与容器化收敛)
- [迭代 02 - Java17 升级与后端启动兼容修复](#迭代-02---java17-升级与后端启动兼容修复)
- [迭代 03 - 登录鉴权链路修复与验证码下线](#迭代-03---登录鉴权链路修复与验证码下线)
- [迭代 04 - 执行计划链路修复与真实案例联调](#迭代-04---执行计划链路修复与真实案例联调)
- [迭代 05 - 个人信息与平台管理页面改造](#迭代-05---个人信息与平台管理页面改造)
- [迭代 06 - 版本2稳定性收口与接口规范统一](#迭代-06---版本2稳定性收口与接口规范统一)
- [迭代 07 - P0持续化治理（全局异常与门禁）](#迭代-07---p0持续化治理全局异常与门禁)
- [迭代 08 - legacy admin 收口与物理清理](#迭代-08---legacy-admin-收口与物理清理)
- [迭代 09 - API命名收口与legacy导入清零](#迭代-09---api命名收口与legacy导入清零)
- [迭代 10 - 门禁可靠性与Playwright最小闭环](#迭代-10---门禁可靠性与playwright最小闭环)
- [迭代 10A - Playwright登录用例稳定性修复](#迭代-10a---playwright登录用例稳定性修复)
- [迭代 11 - Java完善优先与UI协同优化](#迭代-11---java完善优先与ui协同优化)
- [迭代 12 - 个人信息闭环与邮件通知可视化](#迭代-12---个人信息闭环与邮件通知可视化)
- [下一阶段优化任务清单（不含 Python 端）](#下一阶段优化任务清单不含-python-端)
- [日志更新规范（强制）](#日志更新规范强制)
- [迭代记录模板](#迭代记录模板)

---

## 迭代 01 - 部署链路重构与容器化收敛
### 目标与验收
- 目标：形成可稳定复现的本地部署路径，解决“镜像来源不一致、启动失败、前端构建受网络代理影响”问题。
- 验收：`webapp` 与 `exphlp_front` 能正常启动，`/api/auth/healthz` 与 `/api/auth/captcha` 可访问。

### 关键改动
- 将部署相关文件统一收敛到 `docker/`，文档统一收敛到 `docs/`。
- 重写 `docker/docker-compose.yml` 与 `docker/docker-compose.local-front.yml`，支持“全容器构建”和“本地dist兜底”。
- 新增 `docker/scripts/preflight.ps1` 进行镜像、端口、jar产物检查。
- 新增 `docs/新版部署方法.md`，固化启动步骤与故障速查。

### 重难点与报错闭环
- 报错：`base name (${FRONT_NGINX_IMAGE}) should not be blank`
  - 根因：环境变量缺失。
  - 修复：补齐 `.env` 必填键，并在预检脚本强校验。
- 报错：`npm ci ECONNREFUSED 127.0.0.1:7897`
  - 根因：容器内继承了不可用代理。
  - 修复：提供本地构建前端+挂载dist的兜底方案。
- 结果：前端与后端容器可稳定启动，部署流程可复现。

---

## 迭代 02 - Java17 升级与后端启动兼容修复
### 目标与验收
- 目标：后端统一升级到 Java 17 并消除类版本不兼容。
- 验收：`c_exphlp_webapp` 启动成功，Nginx 不再 502。

### 关键改动
- Maven 构建与运行时统一 Java 17。
- Spring 体系升级到兼容 Java17 的版本组合（Boot 2.7.x 等）。
- 增加 `spring.mvc.pathmatch.matching-strategy=ant_path_matcher` 兼容旧 swagger 行为。

### 重难点与报错闭环
- 报错：`Unsupported class file major version 61`
  - 根因：JDK17 编译产物被旧 Spring 版本运行。
  - 修复：升级 Spring Boot/Framework 依赖并重建镜像。
- 结果：`webapp` 容器启动恢复，健康检查通过。

---

## 迭代 03 - 登录鉴权链路修复与验证码下线
### 目标与验收
- 目标：解决“登录后立刻401”“登录状态已过期循环弹窗”“undefined.data”报错；按需求下线验证码。
- 验收：`/api/auth/login -> /api/auth/me` 连续返回 200，登录页无验证码。

### 关键改动
- 前端 `request.js` 在 401 分支显式 `reject`，避免连锁JS异常。
- 登录流程移除验证码字段与接口依赖（`code/uuid/getCodeImg`）。
- 后端 `currentUser` 增加兼容兜底：按 `userId` 查不到时按 `userName` 查。
- 用户主键注解统一为 `@Id`，降低 Mongo 映射差异风险。

### 重难点与报错闭环
- 报错：`TypeError: Cannot read properties of undefined (reading 'data')`
  - 根因：401 分支只弹窗不 reject。
  - 修复：401 分支返回 `Promise.reject` 并跳转 `/login`。
- 报错：`/api/auth/me -> 401 未登录（login已200）`
  - 根因：用户ID查询与历史数据 `_id` 类型不一致。
  - 修复：DAO 双ID兼容 + `currentUser` 按用户名兜底。

---

## 迭代 04 - 执行计划链路修复与真实案例联调
### 目标与验收
- 目标：修复“计划执行失败、planId空、结果查询500”问题，跑通真实案例脚本。
- 验收：`execute` 受理成功，状态可收敛，结果查询接口不再NPE。

### 关键改动
- 修复执行计划实体ID映射与DAO查询逻辑（字符串/ObjectId兼容）。
- 修复 `AlgRltSaveController` 空结果空指针。
- 补充真实案例文档脚本：时间戳命名、planId兜底、失败即停。

### 重难点与报错闭环
- 报错：`execute planId不能为空`
  - 根因：`addExePlan`失败后仍继续执行后续步骤。
  - 修复：脚本增加显式校验与兜底查询。
- 报错：`问题实例不存在: <id>`
  - 根因：问题实例ID查询仍按单一类型匹配。
  - 修复：`ProbInstDao/AlgLibMgrDao` 增加双ID兼容。

---

## 迭代 05 - 个人信息与平台管理页面改造
### 目标与验收
- 目标：修复头像上传401，去掉不合理字段与敏感信息展示，新增手机号/QQ字段。
- 验收：头像上传成功；个人信息与平台管理可读写手机号/QQ；平台管理不再显示密码密文与角色列。

### 关键改动
- 鉴权策略修复：`POST /api/auth/avatar` 需登录；`GET /api/auth/avatar/*` 允许公开读取。
- 个人中心字段整理：移除部门/角色/性别；新增手机号、QQ。
- 平台管理：移除角色列，新增手机号/QQ列；保留重置密码独立操作。
- 后端 `UserInfo` 新增字段：`mobile`、`qq`，并同步 profile/plat 接口返回与更新。

### 重难点与报错闭环
- 报错：提交头像后弹“登录状态已过期”
  - 根因：错误放行 `/api/auth/avatar/**` 导致上传请求无认证上下文。
  - 修复：调整拦截器，仅放行 GET 头像读取。
- 风险：删除角色列后新增/编辑用户可能丢失角色。
  - 处理：后端新增默认角色与更新时角色兜底（沿用原值）。

---

## 迭代 06 - 版本2稳定性收口与接口规范统一
### 目标与验收
- 目标：在 `版本2` 分支统一错误响应结构、补齐关键链路日志字段、完善平台管理字段校验与最小 smoke 回归。
- 验收：认证与计划执行链路返回 `code/msg/errorCode/traceId`；平台管理表单与后端校验一致；smoke 脚本覆盖登录/me/头像/执行计划。

### 关键改动
- 后端：新增统一响应工具（`ApiResponse`）和请求链路标识工具（`TraceContext`），在 `AuthInterceptor/AuthController/ExePlanMgrCtrl/PlatMgrCtrl` 接入。
- 后端：为登录、资料更新、头像上传、计划执行、用户管理写入结构化日志字段（traceId/userId/path/errorCode）。
- 前端：`request.js` 支持 `errorCode` 优先映射与 `message` 兼容，401 弹窗增加并发防抖。
- 前端：平台管理页面新增邮箱/手机号/QQ 规则校验，提交前统一校验。
- 运维：`docker/scripts/smoke.ps1` 增加 `/api/auth/me`、头像上传校验和 `bad-id` 执行计划错误码断言。
- 文档：新增 `docs/错误码映射-v2.md` 与 `docs/接口总览-v2.md`。
- 残留下线（第一批）：移除顶部“消息通知”入口与 `/user/msg` 路由，避免误入依赖 `/admin/*` 的历史消息模块。
- 残留下线（第二批）：禁用动态模板路由注入（固定业务路由模式），并移除 `/dict` 隐藏路由入口，确保 `/admin/*` 历史模块不再通过主导航进入。

### 重难点与报错闭环
- 报错：前后端错误字段不统一（仅 `msg`），导致提示文案不稳定。
  - 根因：接口响应结构未统一，前端仅按 `code/msg` 处理。
  - 修复：后端统一输出 `errorCode + traceId + message`，前端按 `errorCode` 优先映射。
  - 验证：登录失败、无 token、bad-id 执行计划可返回稳定文案与 traceId。
- 报错：平台管理前后端校验规则不一致。
  - 根因：页面缺少完整规则，后端接口无字段校验。
  - 修复：前后端统一邮箱/手机号/QQ/密码长度规则。
  - 验证：非法字段提交被拦截，合法字段可保存。

---

## 迭代 07 - P0持续化治理（全局异常与门禁）
### 目标与验收
- 目标：完成 P0 的持续化治理，防止错误码、日志与回归能力后续回退。
- 验收：全局异常统一返回 `errorCode`；日志包含 `traceId/userId/path/latency`；具备本地与 CI 的固定校验入口。

### 关键改动
- 后端：新增 `ErrorCode` 枚举，控制器统一引用，`ApiResponse.failed` 强制 `errorCode` 必填。
- 后端：新增 `GlobalExceptionHandler`，收敛 `IllegalArgumentException/BusinessException/Exception`。
- 后端：新增 `TraceLoggingFilter`，记录请求耗时与核心结构化字段。
- 前端：请求层阻断 `"/admin/*"` 调用并给出统一下线提示。
- 文档：新增 `/admin/*` 基线清单 `docs/admin-api-legacy-inventory-v2.md`，更新接口与错误码文档。
- 门禁：新增 `scripts/check-p0.ps1` 与 `.github/workflows/p0-check.yml`。

### 重难点与报错闭环
- 报错：历史模板 API 虽无入口但仍可能被代码误调用。
  - 根因：`src/api` 中残留 `/admin/*` 调用量大。
  - 修复：请求拦截器硬隔离并文档化分批删除清单。
  - 验证：调用 `/admin/*` 时前端直接拦截提示，不再发往后端。

---

## 迭代 08 - legacy admin 收口与物理清理
### 目标与验收
- 目标：按批次删除历史模板模块，将 `/admin/*` 从“逻辑隔离”推进到“物理清理”。
- 验收：`rg "/admin/" exphlp-front/src` 仅保留请求拦截器哨兵；前端构建通过；个人中心链路保持可用。

### 关键改动
- 前端：删除不可达 `views/vadmin` 模板页面（monitor/system/tool/permission 非个人中心）。
- 前端：删除对应 `src/api/vadmin` 历史 `/admin/*` API 文件，保留业务主链路 API。
- 前端：个人中心页面迁移到 `src/views/profile`，并新增 `src/api/auth.js` 统一认证/资料接口。
- 前端：移除未使用旧组件 `DeptTree`、`UsersTree`、`ModelDisplay`、`FileUpload` 及其旧调用。
- 门禁：`scripts/check-p0.ps1` 增加 legacy baseline 校验（除 `request.js` 外禁止新增 `/admin/` 引用）。
- 文档：更新 `docs/admin-api-legacy-inventory-v2.md` 与 `docs/接口总览-v2.md`。

### 重难点与报错闭环
- 报错：批量删除后存在“旧路径仍可被 import”风险。
  - 根因：`main.js` 全局注册历史组件，导致旧调用链被打包保留。
  - 修复：移除全局旧组件注册与旧 API 原型挂载，迁移个人中心到新 API 模块。
- 验证：`npm run build:prod` 通过，`/admin/` 搜索仅剩拦截哨兵一处。

---

## 迭代 09 - API命名收口与legacy导入清零
### 目标与验收
- 目标：完成业务页面 `@/api/vadmin/*` 导入迁移，收敛为语义化 API 路径，避免历史命名继续扩散。
- 验收：`rg "@/api/vadmin" exphlp-front/src` 命中为 0；前端构建通过；P0 校验通过。

### 关键改动
- 前端：新增语义化 API 入口目录 `src/api/exphlp/*` 以及 `src/api/problem.js`、`src/api/algorithm.js`。
- 前端：问题实例、算法库、执行计划、平台管理页面导入统一切换到新 API 路径。
- 前端：删除 `src/api/vadmin/{alg,problem,exphlp/*}` 旧业务 API 文件，避免双路径长期并存。
- 门禁：`scripts/check-p0.ps1` 新增 legacy import 校验，禁止 `@/api/vadmin` 回流。

### 重难点与报错闭环
- 报错：迁移中存在“中转导出依赖旧文件”风险。
  - 根因：分批迁移阶段新旧路径短时共存。
  - 修复：完成页面导入切换后，直接物理删除旧 `vadmin` 业务 API 文件。
- 验证：前端构建通过，legacy import 扫描归零。

---

## 迭代 10 - 门禁可靠性与Playwright最小闭环
### 目标与验收
- 目标：修复 `check-p0` 假通过风险，补齐 smoke 的 PowerShell 5.1 兼容性，并接入 Playwright 最小链路测试。
- 验收：任一步骤失败可被门禁真实阻断；smoke 的头像上传步骤通过；Playwright 可执行并包含至少 1 条登录主链路用例。

### 关键改动
- 门禁：`scripts/check-p0.ps1` 改为 fail-fast，原生命令非0退出码会触发失败。
- 门禁：`.github/workflows/p0-check.yml` 同步更新 eslint 目标路径（`views/profile/userInfo.vue` + `src/api/auth.js`）。
- smoke：`docker/scripts/smoke.ps1` 将头像上传改为手工 multipart，兼容 PowerShell 5.1。
- e2e：前端新增 Playwright 配置与首条登录链路测试（Mock `login/me`）。
- CI：新增 `.github/workflows/playwright-e2e.yml`。

### 重难点与报错闭环
- 报错：`check-p0` 历史上可能出现“失败仍 PASS”。
  - 根因：步骤包装函数未校验原生命令退出码。
  - 修复：将退出码检查下沉到各原生命令，并统一异常中断。
  - 验证：`check-p0` 失败可被正确抛出，成功时全链路通过。
- 报错：PowerShell 5.1 不支持 `Invoke-RestMethod -Form`。
  - 根因：参数兼容性差异。
  - 修复：改用 multipart body 手工构造上传文件。
- 验证：`POST /api/auth/avatar` smoke 步骤通过。

---

## 迭代 10A - Playwright登录用例稳定性修复
### 目标与验收
- 目标：修复 Playwright 登录用例在不同路由落点和重复文本场景下的不稳定问题。
- 验收：登录后 URL 断言与首页关键元素断言稳定，不再触发 strict mode 多元素冲突。

### 关键改动
- e2e：登录成功后 URL 断言由固定 `/#/problemModel/index` 调整为兼容 `/#/index` 和 `/#/problemModel/index`。
- e2e：将首页断言从 `getByText("问题实例管理")` 改为侧边菜单链路定位，避免重复文本导致 strict mode 失败。

### 重难点与报错闭环
- 报错：URL 断言失败（实际跳转 `/#/index`）。
  - 根因：断言写死单一路径。
  - 修复：改为正则兼容双路径。
- 报错：`getByText("问题实例管理")` strict mode 冲突。
  - 根因：页面中存在侧边栏、面包屑、标签页三处同文案元素。
  - 修复：改为 `menubar -> link` 唯一路径断言。

---

## 迭代 11 - Java完善优先与UI协同优化
### 目标与验收
- 目标：以 Java 端能力与稳定性完善为先，前端按同链路做交互一致性优化，执行“严格门禁”。
- 验收：每批提交都通过 `check-p0`、smoke、Playwright核心用例（本地受限时以 CI 为准）。

### 关键改动
- 测试与门禁：
  - 新增 `tests/e2e/plan-manage.spec.js`，覆盖“登录 -> 执行计划页 -> 新建计划基础校验”。
  - `check-p0.ps1` 新增 Playwright 核心用例步骤（支持 `-SkipE2E` 开关）。
- Java 稳定性：
  - `ExePlanMgrCtrl/ProbInstMgrCtrl/AlgLibMgrCtrl` 增加分页参数归一化、关键入参非空校验、空列表兜底。
  - `ExePlanMgrDao/ProbInstDao/AlgLibMgrDao` 增加空参数防御与分页边界收敛，减少隐式空返回风险。
- 前端协同优化：
  - 平台管理与问题实例页面增加表格加载态、提交中状态和批量删除并发处理。
- `request.js` 在错误提示中附带 `traceId`，便于后端日志定位。

---

## 迭代 12 - 个人信息闭环与邮件通知可视化
### 目标与验收
- 目标：完善个人中心资料保存的页面一致性，并让“执行结果邮件通知”在执行日志中可见且可排障。
- 验收：个人资料保存后页面信息同步刷新；执行日志出现 `MAIL_NOTIFY` 阶段并明确发送结果。

### 关键改动
- 后端：
  - `EmailService` 返回结构化结果（发送成功/未配置/失败）。
  - `PlanExecuteImpl.notifyPlanResult` 写入 `MAIL_NOTIFY` 日志，记录 `reasonCode` 与失败原因。
  - `docker-compose` 增加 SMTP 环境变量透传（host/port/username/password/auth/starttls）。
- 前端：
  - `profile/userInfo.vue` 保存成功后触发父组件刷新，保证展示区与编辑区一致。
  - `profile/resetPwd.vue` 修改成功后清空输入并提示“建议重新登录”。
- 文档：
  - 用户手册补充 SMTP 配置与 `MAIL_NOTIFY` 排障路径。
  - 错误码映射文档新增“执行日志阶段码（通知相关）”。

### 重难点与报错闭环
- 现象：邮件发送失败或未配置时，用户无感知，只能查服务日志。
  - 根因：邮件发送逻辑未回写执行日志阶段。
  - 修复：在计划执行日志新增 `MAIL_NOTIFY` 阶段，并记录 `reasonCode`。
  - 验证：执行计划完成后可在日志弹窗看到邮件发送状态与原因。

---

## 下一阶段优化任务清单（不含 Python 端）
### P0
- 将 `check-p0.ps1` 纳入团队日常提交流程（提交前必跑）。
- 将 `scripts/check-p0.ps1` 接入 CI 必经门禁（PR 阶段阻断回退）。
- 将 Playwright e2e 扩展到“执行计划最小链路”（登录 -> 计划页 -> 新建计划基础校验）。
- 完成 Java 控制器与 DAO 的边界入参和空返回收口，减少运行时隐式空指针风险。

### P1
- 完善平台管理表单校验与字段约束（邮箱/手机号/QQ）。
- 增加运维脚本：一键 smoke（登录、me、头像上传、计划执行最小链路）并沉淀常见失败排障手册。

---

## 日志更新规范（强制）
1. 每次开发完成后，提交代码前必须追加一条迭代记录。  
2. 若改动涉及接口或数据结构，必须更新“接口变化”描述。  
3. 若出现新报错，必须写清“现象-根因-修复-验证”。  
4. 所有命令与路径使用可复现形式，避免仅截图不留文本。  

---

## 迭代记录模板
```markdown
## 迭代 XX - <主题名称>
### 目标与验收
- 目标：
- 验收：

### 关键改动
- 前端：
- 后端：
- 部署/文档：

### 重难点与报错闭环
- 报错：
  - 根因：
  - 修复：
  - 验证：

### 接口/数据结构变化
- 

### 风险与后续
- 
```
